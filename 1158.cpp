#include <bits/stdc++.h>
using namespace std;

int main() {
	ios::sync_with_stdio(0); cin.tie(0);
	// 배열 , 리스트, 벡터 ... 모두 풀 수 있는 방법이 있음. O(n)이면 풀릴 듯
	// 1번 - list 활용 풀이
	int n,k;
	cin <<n<<k;
	list<int> li;
	auto iter = li.begin();
	//초기화
	for(int i=1;i<=n;i++) li.push_back(i);
	int cnt=0;
	while(iter!=li.end()){
		
	}

	return 0;
}